---
import { cn } from "@/lib/utils";
interface Props {
  className?: string;
}
const { className } = Astro.props;
---

<header class={cn("w-full", className)}>
  <h5 class="uppercase text-4xl font-title">
    <slot />
  </h5>
</header>
